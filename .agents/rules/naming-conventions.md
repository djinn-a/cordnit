# Naming Conventions

Defines the rules for naming files, functions, and variables.

## Constraints
- Match the existing naming conventions found in the repository.
- Do not introduce new casing rules (e.g., snake_case in a camelCase project) without explicit approval.
