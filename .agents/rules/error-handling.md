# Error Handling Rules

Defines how agents should structure error boundaries and handle failures.

## Constraints
- Do not swallow errors silently.
- Ensure all API endpoints and route handlers return structured error responses.
