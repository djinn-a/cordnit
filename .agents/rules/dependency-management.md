# Dependency Management

Defines rules for interacting with external packages and project dependencies.

## Constraints
- Only install dependencies that are explicitly required by the architecture or user.
- Verify compatibility before adding new packages.
