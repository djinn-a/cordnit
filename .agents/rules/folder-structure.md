# Folder Structure Rules

Defines constraints around the project's directory hierarchy.

## Rule: Existing Structure
**EXISTING STRUCTURE → NEW STRUCTURE**
Always prefer placing new files in the existing directory structure before inventing new domains.

## Constraints
- Never create arbitrary folders.
- Never move files without checking dependencies.
